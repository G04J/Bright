services:
  db:
    image: postgres:16
    container_name: bright-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bright
    ports:
      - "5432:5432"
    volumes:
      - bright_pgdata:/var/lib/postgresql/data

volumes:
  bright_pgdata:
